# SmithersBot README Claim Matrix

One row per claim (1..18), in order. Status drawn from `{verified, partial, unverified, roadmap, manual-required}`. Runtime status from `artifacts/INDEX.md` is authoritative; when runtime and code-survey evidence disagree the weaker classification has been kept rather than overclaiming.

| # | Claim | Status | Evidence | Safe README wording | Unsafe wording to avoid |
| --- | --- | --- | --- | --- | --- |
| 1 | Goal creation (CLI plan-only) | verified | `artifacts/claim-01-cli-help.log` (`node scripts/run-node.mjs goal --help` exit 0, top-level form `moltbot goal [options] [command] [goal]`, no `new` subcommand); `artifacts/claim-01-cli-plan.log` (`MOLTBOT_STATE_DIR=$PWD/RELEASE_AUDIT/README_VERIFY/_state node scripts/run-node.mjs goal "Add a short docstring to scripts/run-node.mjs" --plan-only --output json` exit 0, persisted run `5e1de960-…` with `run.json` + `scout/` set under the redirected state dir, nothing under `~/.moltbot/`); `src/commands/goal.ts:90/236/286`. | Create a goal from the CLI with `moltbot goal "<task>" --plan-only`; the plan is persisted on disk and held for approval. `MOLTBOT_STATE_DIR` redirects goal state to any directory. | `moltbot goal new "<task>" --plan-only` (no `new` subcommand). "Plans always render to stdout" (with `--output json --plan-only` stdout is intentionally empty; plan lives on disk). |
| 2 | Telegram goal creation (`/new_goal`) | partial | `artifacts/claim-02-telegram.log` (static-only inspection, no live Telegram send); `src/telegram/goal-commands.ts:1959-1960` (handler), `:97-99` (command spec), `:484-487` (usage); `src/telegram/bot-handlers.ts:54-58` (help text); `src/telegram/goal-commands.test.ts:3094/3205` (handler test coverage). | Telegram-controlled: send `/new_goal <description>` to the bot to start a goal; approve, request changes, or reject from inline buttons. | "Verified end-to-end via Telegram in this release audit" (no live message was sent in this audit). "Fully automated Telegram approval" (operator action is still required). |
| 3 | Structured planning (typed steps, deps, backends, criteria, constraints, build-gate) | verified | `artifacts/claim-03-structured-plan.log` (persisted plan in `_state/.../run.json` has `id`, `dependsOn: []`, `backend: "codex"`, `successCriteria`, `constraints` (4 items), `status: "pending"`, `durationMinutes`, plan-level `buildGate: { commands: ["node --check scripts/run-node.mjs"], runBetweenSteps: false }`); `src/goal/planner.ts:14`, `src/goal/types.ts:57`, `src/goal/goal-schemas.ts:50`. | Plans are validated structured objects: typed steps, explicit `dependsOn`, per-step worker backend, success criteria, constraints, and a project build/test gate. | "Plans are guaranteed to be correct" (schema checks contract, not semantic correctness). "Every step has success criteria" (prompted but optional in schema). |
| 4 | DAG / critical path | verified | `artifacts/claim-04-dag.log` (`MOLTBOT_STATE_DIR=… node scripts/run-node.mjs goal detail 5e1de960-…` exit 0; emitted both an ASCII dependency block and a Mermaid `flowchart TD` source block); PNG path is wired only into Telegram at `src/telegram/goal-sending.ts:800` via `src/goal/mermaid-png.ts:33`; `src/goal/dag-render.ts:1/28/62`, `src/goal/cpm.ts:57`. | `moltbot goal detail <run_id>` renders a text DAG and a Mermaid source block; **text DAG verified only** on the CLI, while the Telegram channel additionally renders the DAG to PNG. | "Renders a graphical PNG dependency graph in the terminal" (false for the CLI). "Renders a Mermaid diagram out of the box on every channel" (unverified beyond Telegram). |
| 5 | Worker orchestration (multiple local CLI backends) | verified | `artifacts/claim-05-workers.log` (codex transitively exercised by the plan-only run; static inspection only); `src/goal/backend-types.ts:7-10` (`codex`, `claude_code`); `src/goal/backend-availability.ts:114-148` (PATH probes); `src/goal/cli-worker.ts:183/249/288` (dispatch). | SmithersBot can route work to local Codex or Claude Code CLI workers; whichever is installed on PATH is probed at startup and assigned work. | "Parallel multi-agent execution verified" (execution is sequential in this executor). "Both Codex and Claude Code are required" (the harness clamps gracefully to whatever is available). |
| 6 | Artifact trail (prompts, stdout/stderr, result JSON, attempts, journals, run state) | verified | `artifacts/claim-06-artifact-trail.log` (for the plan-only run, `run.json` + `scout/` set on disk: `PLANNING_BRIEF.md`, `plan_draft.md`, `execution_plan.json`, `scout_report.json`, `planning_stdout.txt`, `planning_stderr.txt`, `planning_raw_output.txt`, `attempt-1.json`, `auth_mode.txt`, `node_specs/*.md`); `src/goal/cli-worker.ts:234/273-276/294-295`; `src/goal/run-journal.ts:14-78`; `src/goal/attempt-bundle.ts:35-36`; `src/goal/planner.ts:146`. | Every plan, worker prompt, stdout/stderr capture, attempt bundle, journal note, and run state file lives on disk under the goals state directory and can be inspected after the fact. | "Captures every stdin/stdout byte of every subprocess" (true for worker CLIs but easy to overclaim). "Writes a database log" (no DB; flat files only). |
| 7 | Checkpoint / recoverability | verified | `artifacts/claim-07-checkpoints.log` (static inspection only; explicit "local checkpoint commits"; no `git push` from the per-task loop); `src/goal/git-checkpoint.ts:289-294` (`autosaveIfDirty`), `:296-309` (`startTaskCheckpoint`), `:311-322` (`finalizeTaskCheckpoint`), `:193-209` (`ensureRunBranch`); `src/goal/build-gate.ts:195-211` (`resetToTaskBaseSha`); `src/goal/agent-executor.ts:388/465/499/609`. | Before each task starts, SmithersBot makes a **local checkpoint commit** on a per-run branch so a failed worker can be reverted to the pre-step state via a **recoverability checkpoint** reset. | "Automatically pushes to git after each step" (false). "Auto-pushes to your origin" (false). "Uses git stash" (not the mechanism in use). "Snapshots your entire repo" (commits dirty tracked state, not ignored files). |
| 8 | Build gate (system blocks on real subprocess failure) | verified | `artifacts/claim-08-build-gate.log` (full citation trail; system blocks on real subprocess exit, not LLM self-assessment); `src/goal/build-gate.ts:65-115` (`runBuildGateCommands` via `spawnSync`); `src/goal/agent-executor.ts:565-661` (overrides worker `done` to `blocked`/`pending` on failure); `src/goal/agent-executor.ts:739-761` (session `done` conditional on final gate). | Code-related steps cannot complete until the operator-configured build/test commands actually exit zero; SmithersBot runs the commands itself with `spawnSync` and captures full stdout/stderr. | "The system trusts the model when it says tests pass" (the opposite is true). "Sandbox executes commands safely" (the gate runs commands as configured; it does not sandbox them). |
| 9 | Manual test recommendations with criticality | verified | `artifacts/claim-09-manual-tests.log` (static inspection; criticality 1..10 scale, fail-open extraction); `src/goal/manual-tests.ts:8-62/168-172/224-226/327-399`; `src/goal/types.ts:123-125` (`ManualTestSuggestion`); `src/goal/agent-executor.ts:52/1036-1048/1061-1069`; `src/telegram/goal-formatting.ts:96`; `src/telegram/goal-sending.ts:275-288`. | After a goal completes, SmithersBot suggests the manual smoke tests it could not run itself, each ranked by an estimated 1..10 criticality. | "Calibrated risk score" (it is a 1..10 LLM heuristic, not a calibrated risk model). "Guaranteed test coverage" (manual tests are suggestions). |
| 10 | Blocked-loop behavior (keep running until blocked, then report) | verified | `artifacts/claim-10-blocked-loop.log` (static citation trail); `src/goal/agent-executor.ts:252` (`orderStepsCriticalPathFirst`); `:310/333-336/1158-1177` (loop, `findRunnableTasks`, `pickNextTask`); `:340-350` (operator-answer revive); `:704-714` (`step_blocked`); `:695-702/716-726` (fatal vs recoverable); `:1074-1109` (`fully_blocked` event + `session.state = "blocked"`). | Steps run until their dependencies are met or until they hit a blocker; SmithersBot keeps running unrelated parts of the DAG, then reports remaining blockers to the operator with a consolidated question. | "Parallel workers" (execution is sequential in this executor). "Always retries until success" (fatal errors stop the run by design). |
| 11 | Recovery loops (admit stuck → revert → retry → escalate) | partial | `artifacts/claim-11-ralph-recovery.log` (stuck-loop detection, revert-to-checkpoint, persist failure, retry with appended context, escalate to operator — no automatic backend handoff); `src/goal/cli-worker.ts:170/659-662/1034`; `src/goal/types.ts:50-54`; `src/goal/agent-executor.ts:82/192/477-530/499`; `src/goal/git-checkpoint.ts:7-8/296/311`; `src/goal/run-journal.ts:46-78/80-90`; `src/goal/lessons.ts:35/39/415-453/487-491`. | When a worker gets stuck, SmithersBot reverts the working tree to the pre-step recoverability checkpoint, records what failed, and retries the task with new context; if still stuck after a configurable retry budget, it escalates to the operator with the full failure history. | "Hands off context to another AI" (there is no automatic multi-backend handoff). "Self-healing" (recovery is bounded by a retry limit). Any use of the internal token *ralph* in user-facing copy. |
| 12 | Nightwatch | verified | `artifacts/claim-12-nightwatch.log` (real cron job `nightwatch-daily`, registered Telegram command, unit + integration tests); `src/cron/nightwatch.ts:30-37/594-655/658-725/727-775`; `src/telegram/nightwatch-commands.ts:17-32`; `src/telegram/bot-native-commands.ts:54-56/174/185/530-533`. | **Nightwatch** is a scheduled daily code review that runs in the background and delivers a summary plan to your configured Telegram chat; schedule and chat are configurable through `/nightwatch`. | "Runs at exactly 3am UTC" (schedule is configurable). "Reviews production deploys" (Nightwatch runs against a working directory, not a deploy target). |
| 13 | Per-directory agent memory | partial | `artifacts/claim-13-per-dir-memory.log` (CLAUDE.md reaches Codex via explicit harness injection and Claude Code via native cwd discovery; AGENTS.md not read by the goal worker; MEMORY.md belongs to a different subsystem); `src/goal/cli-runner.ts:37-38/76-84`; `src/goal/cli-worker.ts:60/231/267-272/284/700`; `src/goal/cli-runner.test.ts:70-82`, `src/goal/cli-worker.test.ts:294`; `src/goal/lessons.ts:174-180`; `src/goal/cli-worker.ts:540-582`. | Each working directory can carry its own `CLAUDE.md`; SmithersBot also remembers per-directory and global lessons and injects the relevant ones into the worker prompt under a labelled section. | "Reads `CLAUDE.md`, `AGENTS.md`, and `MEMORY.md` in every working directory" (only `CLAUDE.md` is plumbed by the goal worker). "Cross-machine memory" (lessons live in the local state directory). |
| 14 | Semgrep / security scan | verified | `artifacts/claim-14-semgrep.log` (default ON; per-step cadence; path-scoped scan prepended after each completed step; whole-repo scan on the final gate when cadence is `goal`); `src/goal/build-gate.ts:7` (`DEFAULT_SAST_SEMGREP_ENABLED=true`); `src/goal/agent-executor.ts:179/539-555/732-737`; `src/goal/build-gate.ts:160-192` (command shape); `src/types.goal.ts:3/31-32` (operator toggle `off`/`step`/`goal`); `src/goal/agent-executor.test.ts:902/970/1004/1046`. | After each code-related step SmithersBot runs Semgrep against changed files (cadence configurable to `off`, `step`, or `goal`); a failed scan blocks the step the same way any other build-gate command failure does. | "Replaces a full security review" (Semgrep is a SAST scan, not a pen test). "Always offline" (`--config auto` may fetch rules from the network and can fail as infra). |
| 15 | Claude Code / Codex CLI backends (subscription) | verified | `artifacts/claim-15-subscriptions.log` (real subprocess invocations of the local `codex` and `claude` binaries; subscription default; credential env scrub); `src/goal/backend-types.ts:7-10`; `src/goal/cli-worker.ts:183/288/327-336/782-816`; `src/types.goal.ts:1` (`ClaudeCodeAuthMode`); `src/goal/cli-worker.ts:230` (default `subscription`); `src/goal/claude-code-env.ts:7-11/54-67` (strips `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN`/`ANTHROPIC_API_KEY_OLD`); `src/goal/cli-worker.ts:250` (`auth_mode.txt`). | SmithersBot **uses local CLI backends** (Codex and Claude Code), so it runs against whichever logins are already installed on the operator's machine. | "Free unlimited Claude" / "free Codex" (the harness does not promise free or unlimited use). "No API keys ever" (`api_key` mode also exists; subscription mode just strips Anthropic credential env vars). |
| 16 | Guardrails (hard-deny / capability enforcement) | verified | `artifacts/claim-16-guardrails.log` (9/9 PASS in `claim-16-denial-probe.mjs`, exit 0: HardDeny match for `rm -rf /`, `sudo cat /etc/shadow`, `npm publish`, `kubectl apply …`, `.env`, `~/.ssh/id_rsa`, `credentials.json`; `null` for `echo hello world` and `README.md`; no dangerous command executed); `src/goal/hard-deny.ts:7/26/41/52/121/150/205/1250/1302`; `src/goal/capability-enforcement.ts:44/50-56/66/80/93/103/115`. | Worker tool calls run through a typed deny check that blocks reads/writes to sensitive paths (env files, SSH keys, credentials) and a recursive command scanner that blocks dangerous shell forms, elevated-privilege commands, and publish/deploy/release commands. | "Sandbox" (there is no OS-level isolation; this is policy-and-scan enforcement). "Cannot leak secrets" (credential env stripping reduces risk, it does not eliminate it). |
| 17 | Lessons / memory injection | verified | `artifacts/claim-17-lessons-injection.log` (this worker's own prompt contains the labelled `LESSONS FROM PRIOR RUNS` block — direct end-to-end evidence); `src/goal/lessons.ts:25/35/42/66/74/85/104/127/155/174/581/590/600/614/629`; `src/goal/cli-runner.ts:33-52`; `src/goal/cli-worker.ts:540-582/576-582`. | Completed runs can extract scoped lessons; later workers in the same working directory or globally automatically receive the relevant lessons in their prompt under a labelled section. | "Self-improving AI engineer" (explicitly forbidden by the product-definition). "Shared lessons across machines" (lessons live in the local state directory). "Every run produces lessons" (extraction is fail-open). |
| 18 | Resume / crash recovery | verified | `artifacts/claim-18-resume-recovery.log` (atomic on-disk persistence, executing-without-live-process reset to blocked, gateway startup sweep, resume reads reconciled state); `src/goal/run-store.ts:31-47/66-151/223-247`; `src/gateway/server-startup.ts:43-50`; `src/commands/goal-resume.ts:340/360`. | If the gateway crashes mid-run, the next start reconciles stale in-progress steps; `moltbot goal resume <run_id>` continues from the persisted run state on disk. | "Guaranteed crash recovery" (best-effort, not a formal guarantee). "Distributed/HA" (state coordination is local-filesystem only). |

Sources: `RELEASE_AUDIT/README_VERIFY/artifacts/INDEX.md`, `RELEASE_AUDIT/README_VERIFY/_evidence/code-survey.md`, and `RELEASE_AUDIT/README_VERIFY/artifacts/claim-01..18-*.log`.
