## Goal creation & planning

Authorship signal: local git history shows the assigned planner files and the Telegram goal entrypoint were added in SmithersBot/goal-system commits, not inherited Moltbot code. Relevant add-history includes `5d98307b8 2026-01-29 M O Add goal command: plan -> approve -> execute in sandbox`, `daf16fea4 2026-02-01 M O milestone 7+8: agentic goal execution with PI session, task-level blocking semantics`, `fceccec1d 2026-02-12 M O claw: unified-cli-planner`, and `3567438de 2026-02-13 M O claw: impl-autocheck-logic`.

| Sub-area | Feature name | What it does | Evidence | Upstream or mine | Maturity | README relevance | Risks or caveats |
|---|---|---|---|---|---|---|---|
| Goal creation flow | `/new_goal` Telegram planning flow | Registers `/new_goal` (with `/goal` alias), authenticates the caller, runs goal planning in the background, sends an immediate acknowledgement/typing loop, and returns the plan for approval instead of executing immediately. The handler calls `handleGoal`, which creates a run id, invokes `goalCommand` with `planOnly: true`, optionally runs autocheck, then marks the run `awaiting_approval` after delivery. | `src/telegram/goal-commands.ts:97` lists `new_goal`; `src/telegram/goal-commands.ts:483` defines `handleGoal`; `src/telegram/goal-commands.ts:494` calls `goalCommand` with `planOnly: true`; `src/telegram/goal-commands.ts:527` runs autocheck before delivery; `src/telegram/goal-commands.ts:555` marks the run awaiting approval; `src/telegram/goal-commands.ts:1959` registers `/new_goal`; `src/telegram/goal-commands.ts:1971` sends it through `runGoalInBackground`; `src/commands/goal.ts:90` defines `goalCommand`; `src/commands/goal.ts:121` creates in-memory planning state; `src/commands/goal.ts:150` persists the run before planning; `src/commands/goal.ts:257` documents plan-only handoff to channel adapters. | definitely mine | working | core | Public claims should say "Telegram control surface" only if the public v0 intends to expose Telegram; CLI planning exists too, but this row is Telegram-specific. Runtime behavior was not exercised in this read-only audit. |
| Goal creation flow | CLI goal command run persistence and approval gate | Creates a goal session, resolves/creates a working directory, persists the run before planning, records planner metadata, displays a formatted plan, and either stops in plan-only mode or transitions into an approval gate before execution. | `src/commands/goal.ts:102` resolves cwd/workingDir; `src/commands/goal.ts:121` initializes `GoalSession` with state `planning`; `src/commands/goal.ts:131` serializes and saves run state; `src/commands/goal.ts:158` starts the planning pass; `src/commands/goal.ts:213` handles blocked-at-planning clarification; `src/commands/goal.ts:236` stores the returned plan; `src/commands/goal.ts:243` renders the plan; `src/commands/goal.ts:286` enters the approval gate. | definitely mine | working | core | Evidence confirms code structure, not live success. The command surface name/arguments should be verified separately before README examples. |
| Planner LLM call | Structured planner prompt and schema contract | Defines the planning prompt for autonomous coding-agent tasks, requiring JSON output with workingDir, summary, buildGate, step dependencies, backend assignment, successCriteria, and constraints. It also supports blocked clarification responses when planning needs more information. | `src/goal/planner.ts:14` begins `PLAN_SYSTEM_PROMPT`; `src/goal/planner.ts:34` requires backend selection; `src/goal/planner.ts:42` requires structured success criteria/constraints/build gate; `src/goal/planner.ts:83` specifies the raw JSON response schema; `src/goal/planner.ts:126` defines the blocked clarification response; `src/goal/types.ts:57` defines `PlanStep`; `src/goal/types.ts:97` defines `PlanBuildGate`; `src/goal/types.ts:104` defines `Plan`; `src/goal/goal-schemas.ts:33` defines build-gate input schema; `src/goal/goal-schemas.ts:39` defines step input schema; `src/goal/goal-schemas.ts:50` defines plan input schema. | definitely mine | working | core | Prompt claims are source-backed, but quality claims about generated plans need demo evidence. The planner prompt is specific to coding goals and may overpromise outside code repositories. |
| Planner LLM call | Unified CLI planner with scout artifacts and backend fallback | Runs a single CLI planning pass through Claude Code or Codex, writes planning/scout artifacts, validates scout output, falls back from Claude to Codex on Anthropic usage/rate limits when enabled, parses either the canonical `execution_plan.json` or stdout, and writes a canonical plan artifact. | `src/goal/cli-planner.ts:33` sets planner timeout; `src/goal/cli-planner.ts:36` documents canonical artifacts under `<run>/scout`; `src/goal/cli-planner.ts:48` appends execution-plan requirements to scout; `src/goal/cli-planner.ts:155` builds Codex planning args; `src/goal/cli-planner.ts:171` resolves enabled planner backends; `src/goal/cli-planner.ts:251` builds planning prompts; `src/goal/cli-planner.ts:338` parses plan with file/stdout fallback; `src/goal/cli-planner.ts:574` defines `runCliPlanning`; `src/goal/cli-planner.ts:599` builds Claude/Codex prompts; `src/goal/cli-planner.ts:641` invokes `runCliProcess`; `src/goal/cli-planner.ts:684` detects degraded Claude planning and falls back; `src/goal/cli-planner.ts:730` validates scout artifacts; `src/goal/cli-planner.ts:776` parses the plan; `src/goal/cli-planner.ts:816` rewrites degraded backend assignments; `src/goal/cli-planner.ts:819` writes canonical plan artifact. | definitely mine | working | core | Uses local CLI tools and long timeouts; public README should avoid implying hosted/SaaS planning. Degraded fallback depends on configured enabled workers and installed CLIs. |
| Planner LLM call | Legacy/direct LLM planner parser | Provides a client-agnostic `generatePlan` API for direct LLM completion, including prompt construction, scout-data enrichment, JSON extraction/repair, blocked-response handling, schema validation, backend normalization, duplicate-step checks, and path normalization. | `src/goal/planner.ts:157` builds planner user messages; `src/goal/planner.ts:184` calls `client.complete`; `src/goal/planner.ts:210` parses response text; `src/goal/planner.ts:223` extracts JSON from raw text/fences; `src/goal/planner.ts:268` repairs malformed JSON; `src/goal/planner.ts:301` defines valid backend IDs; `src/goal/planner.ts:377` validates the plan; `src/goal/planner.ts:428` checks duplicate step ids; `src/goal/planner.ts:454` requires a valid backend. | definitely mine | partial | mention | This looks like a reusable parser/API and has tests, but the main flow now uses `runCliPlanning`; avoid positioning this as the primary public interface unless verified. |
| Plan autocheck / validation | Independent plan autocheck reviewer loop | Runs an independent read-only reviewer against the generated plan, including formatted plan output, Mermaid DAG, JSON snapshot, prior feedback, and user edit instructions. The reviewer returns decision JSON; rejected plans trigger a planner revision loop up to a configured max, with round artifacts persisted under `<run>/autocheck`. | `src/goal/plan-autocheck.ts:28` defines reviewer instructions/checklist; `src/goal/plan-autocheck.ts:61` requires decision JSON; `src/goal/plan-autocheck.ts:74` defines autocheck params; `src/goal/plan-autocheck.ts:91` defines autocheck result; `src/goal/plan-autocheck.ts:181` parses reviewer decision objects; `src/goal/plan-autocheck.ts:445` builds the review prompt; `src/goal/plan-autocheck.ts:536` runs reviewer attempts; `src/goal/plan-autocheck.ts:640` starts the autocheck loop; `src/goal/plan-autocheck.ts:663` iterates rounds; `src/goal/plan-autocheck.ts:776` writes prompt/response/session/backend/metadata artifacts; `src/goal/plan-autocheck.ts:807` enforces max rounds; `src/goal/plan-autocheck.ts:819` calls `runCliPlanRevision`; `src/goal/plan-autocheck.ts:867` commits accepted revisions. | definitely mine | working | core | Autocheck can auto-approve after fresh fallback failure to avoid blocking (`src/goal/plan-autocheck.ts:735`), so README should not claim plans are always independently validated unless that caveat is stated or behavior is verified/configured. |
| Plan autocheck / validation | Telegram-configurable plan autocheck mode | Exposes `/goal_plan_autocheck <codex|claude_code|off>` in Telegram, persists the mode to config when channel config writes are allowed, and wires enabled autocheck into `handleGoal` before the user sees the plan. | `src/telegram/goal-commands.ts:103` lists `goal_plan_autocheck`; `src/telegram/goal-commands.ts:189` defines usage; `src/telegram/goal-commands.ts:230` wraps `runPlanAutocheck`; `src/telegram/goal-commands.ts:245` passes plan/goal/mode/workingDir/runDir to autocheck; `src/telegram/goal-commands.ts:260` commits autocheck revisions; `src/telegram/goal-commands.ts:278` saves autocheck metadata on the run; `src/telegram/goal-commands.ts:527` calls autocheck during `handleGoal`; `src/telegram/goal-commands.ts:2035` registers the command; `src/telegram/goal-commands.ts:2055` parses the requested mode; `src/telegram/goal-commands.ts:2069` gates config writes; `src/telegram/goal-commands.ts:2081` persists the config. | definitely mine | working | mention | Configuration write availability depends on Telegram account/channel policy. Public v0 likely should describe autocheck as a capability, not lead with Telegram config commands. |
| Plan autocheck / validation | Plan schema and validation contract | Validates planner output with Zod-backed schemas and normalizes user-facing plan fields into internal `Plan`/`PlanStep` structures, including required non-empty workingDir/steps/backend and optional build-gate configuration. | `src/goal/goal-schemas.ts:33` validates build gate shape; `src/goal/goal-schemas.ts:39` validates planner step input; `src/goal/goal-schemas.ts:50` validates planner plan input; `src/goal/planner.ts:377` invokes `PlanInputSchema.safeParse`; `src/goal/planner.ts:383` gives targeted workingDir error; `src/goal/planner.ts:386` rejects empty step lists; `src/goal/planner.ts:396` rejects missing/invalid backend; `src/goal/planner.ts:413` parses build gate; `src/goal/planner.ts:418` expands `~` workingDir; `src/goal/planner.ts:431` iterates and normalizes steps. | definitely mine | working | core | Validation proves syntactic/contract checks, not semantic correctness. Some fields like `successCriteria` and `constraints` are prompted as required but schema currently treats them optional (`src/goal/goal-schemas.ts:43`), so public copy should avoid overstating strict enforcement there. |
| Goal-specific tools | PI-agent goal tool signals | Defines PI-agent task-completion, user-input, and failure signaling tools plus working-directory path resolution. This supports later execution more than planning, but it is part of the goal system source set requested for this slice. | `src/goal/goal-tools.ts:43` resolves safe workspace-relative paths; `src/goal/goal-tools.ts:53` defines `GoalToolSignal`; `src/goal/goal-tools.ts:76` creates goal tools; `src/goal/goal-tools.ts:100` defines `mark_task_complete`; `src/goal/goal-tools.ts:135` defines `request_user_input`; `src/goal/goal-tools.ts:162` defines `mark_task_failed`. | definitely mine | working | mention | Not a public planning feature by itself; better covered under worker/orchestration inventory. |

## Workers & orchestration

Authorship signal: local git history for `src/goal/cli-worker.ts`, `src/goal/cli-runner.ts`, `src/goal/backend-availability.ts`, `src/goal/backend-types.ts`, `src/goal/agent-executor.ts`, `src/goal/agent-executor-helpers.ts`, and `src/goal/task-runner.ts` is dominated by SmithersBot/goal-system commits rather than upstream Moltbot code. Relevant add/change history includes `daf16fea4 milestone 7+8: agentic goal execution with PI session, task-level blocking semantics`, `0879644b2 Autonomous Agents Work`, `2ca4a5d60 claw: worker-prompt-and-ralph`, `4a8c2dbcb claw: executor-ralph-and-buildgate`, `cab6f67ea claw: adapt-executor-fallback`, `a4c22dae2 claw: refactor-with-zod`, `451825df0 claw: fix-answer-consume-loop`, and `a742b55ca claw: remove-cache`.

| Sub-area | Feature name | What it does | Evidence | Upstream or mine | Maturity | README relevance | Risks or caveats |
|---|---|---|---|---|---|---|---|
| Worker assignment | Critical-path-aware task assignment | Computes critical-path scores, orders steps by critical-path priority, builds successor maps, finds dependency-ready runnable steps, and picks the next task using score, successor locality, and original plan order. Blocked steps can re-enter the runnable set when a user answer exists or when the blocked reason is retryable on resume. | `src/goal/agent-executor.ts:248` computes critical-path scores; `src/goal/agent-executor.ts:249` builds the original order index; `src/goal/agent-executor.ts:250` builds successors; `src/goal/agent-executor.ts:252` orders steps critical-path-first; `src/goal/agent-executor.ts:333` calls `findRunnableTasks`; `src/goal/agent-executor.ts:336` calls `pickNextTask`; `src/goal/agent-executor-helpers.ts:156` builds the successor map; `src/goal/agent-executor-helpers.ts:170` defines `pickNextTask`; `src/goal/agent-executor-helpers.ts:185` filters max-score candidates; `src/goal/agent-executor-helpers.ts:186` prefers successors of the last executed task; `src/goal/agent-executor-helpers.ts:194` falls back to original order; `src/goal/agent-executor.ts:1158` defines dependency-aware runnable filtering; `src/goal/agent-executor.ts:1171` allows answered/retryable blocked steps to run again. | definitely mine | working | core | Execution is sequential in this file even though the plan can model a DAG; public copy should say "DAG/critical-path scheduling" rather than "parallel execution" unless a separate parallel runner is verified. |
| Worker status/availability | Backend probing and availability gating | Defines goal execution backends (`pi`, `codex`, `claude_code`), resolves enabled CLI workers from config, probes local CLI availability with safe help/no-op checks, records unavailable reasons, and blocks a task before execution if the chosen backend is missing or unsupported. | `src/goal/backend-types.ts:8` defines `GoalBackendId`; `src/goal/backend-types.ts:10` defaults enabled workers to Codex and Claude Code; `src/goal/backend-types.ts:12` resolves configured enabled workers; `src/goal/backend-types.ts:20` defines `BackendAvailability`; `src/goal/backend-availability.ts:74` defines probe handling; `src/goal/backend-availability.ts:102` detects Codex approval-flag placement; `src/goal/backend-availability.ts:114` probes all backends; `src/goal/backend-availability.ts:121` marks Codex flag probe as safe/no-op; `src/goal/backend-availability.ts:150` checks a backend result; `src/goal/agent-executor.ts:273` detects backend availability; `src/goal/agent-executor.ts:359` checks the selected backend before running; `src/goal/agent-executor.ts:360` blocks unavailable backends with a user-facing message. | definitely mine | working | core | Availability probes only prove local binaries/flags are present, not auth quality or enough credits. The `pi` backend is always marked available in this probe (`src/goal/backend-availability.ts:115`), so public claims should distinguish built-in PI runner from external CLI availability. |
| Backend routing | Configurable backend routing and fallback | Resolves each step's backend from a serialized override, previous executed backend, planned backend, or default; clamps unavailable planned CLI backends to the configured enabled worker set; rewrites Claude-assigned steps to Codex when the planner degraded due to Anthropic limits; and instantiates only the enabled CLI runners plus the built-in PI runner. | `src/goal/agent-executor-helpers.ts:9` defines backend resolution priority; `src/goal/agent-executor-helpers.ts:20` clamps to enabled CLI workers; `src/goal/agent-executor.ts:273` reads serialized backend override/degraded planner state; `src/goal/agent-executor.ts:278` chooses the default CLI backend; `src/goal/agent-executor.ts:280` chooses Codex after planner degradation when enabled; `src/goal/agent-executor.ts:283` rewrites step backend assignments for degraded planning; `src/goal/agent-executor.ts:289` creates the PI runner; `src/goal/agent-executor.ts:297` initializes CLI runner slots; `src/goal/agent-executor.ts:298` creates a Codex runner only when enabled; `src/goal/agent-executor.ts:301` creates a Claude Code runner only when enabled; `src/goal/agent-executor.ts:355` resolves and clamps the backend for a task; `src/goal/agent-executor.ts:372` records `executedBackend`; `src/goal/agent-executor.ts:374` chooses the PI or CLI runner. | definitely mine | working | core | Backend fallback is local-tool dependent. The default backend constant is Claude Code (`src/goal/agent-executor.ts:64`), but degraded planner behavior can rewrite to Codex; README examples should not imply every installation has both tools configured. |
| Task orchestration loop | Per-task execution, retries, ralph handling, and status recording | Runs a loop until no tasks are runnable or execution is stopped, resets stale `in_progress` steps, constructs a `TaskRunnerContext` with plan, task, hard denies, completed summaries, resume data, attempts, progress callbacks, abort signal, and timeout, then executes the selected runner. Results are applied to step state, retryable failures get attempt context and a retry, ralph responses reset or block based on limits, and final task results are recorded and journaled. | `src/goal/agent-executor.ts:309` starts the execution loop; `src/goal/agent-executor.ts:313` checks for external cancellation; `src/goal/agent-executor.ts:320` resets stuck `in_progress` steps; `src/goal/agent-executor.ts:338` handles blocked-step resume answers; `src/goal/agent-executor.ts:352` calculates task timing; `src/goal/agent-executor.ts:402` sets retry controls; `src/goal/agent-executor.ts:407` iterates attempts; `src/goal/agent-executor.ts:414` builds `TaskRunnerContext`; `src/goal/agent-executor.ts:430` marks the task `in_progress`; `src/goal/agent-executor.ts:436` executes the runner; `src/goal/agent-executor.ts:438` applies the task result; `src/goal/agent-executor.ts:442` retries retryable outcomes; `src/goal/agent-executor.ts:477` handles ralph results; `src/goal/agent-executor.ts:679` records task results; `src/goal/agent-executor.ts:684` journals done tasks; `src/goal/agent-executor.ts:686` journals task failures; `src/goal/agent-executor-helpers.ts:29` maps runner results onto `PlanStep` status; `src/goal/agent-executor-helpers.ts:70` decides retry eligibility; `src/goal/agent-executor-helpers.ts:90` records duration/outcome updates. | definitely mine | working | core | Loop evidence is strong, but this read-only audit did not execute a live goal. There are many side paths (build gate, post-review, git checkpointing) that should be inventoried separately before making broad "self-healing" claims. |
| Task orchestration loop | TaskRunner abstraction across PI and CLI workers | Defines a narrow `TaskRunner` interface and common context/result contract, allowing the executor to call PI and CLI workers uniformly while each runner maps backend-specific output into shared statuses (`complete`, `blocked`, `failed`, `ralph`). | `src/goal/task-runner.ts:5` defines `TaskRunnerContext`; `src/goal/task-runner.ts:21` defines shared `TaskRunnerResult`; `src/goal/task-runner.ts:32` defines the `TaskRunner` interface; `src/goal/cli-runner.ts:11` implements `CliTaskRunner`; `src/goal/cli-runner.ts:40` calls `executeTaskWithCliWorker`; `src/goal/cli-runner.ts:86` maps validated worker output; `src/goal/cli-runner.ts:140` maps worker error types to executor blocked reasons; `src/goal/agent-executor.ts:289` constructs `PiTaskRunner`; `src/goal/agent-executor.ts:297` stores CLI runners as `TaskRunner`s. | definitely mine | working | mention | PI runner was not part of this assigned read set beyond construction sites, so claims about PI-specific behavior should be made from the PI inventory slice, not this row. |
| Worker assignment | CLI worker prompt and result protocol | Generates a self-contained worker prompt that names the goal, full plan context, prior lessons, completed tasks, resume context, exact task, success criteria, constraints, hard denies, prior attempt summary, verification instruction, and required `worker_result.json` protocol. The executor writes prompt/capability artifacts, runs the selected CLI, polls for the result file, validates the schema, repairs invalid result JSON once, copies the canonical result artifact, and writes an attempt bundle with logs, diffstat, changed files, and ralph detail. | `src/goal/cli-worker.ts:252` builds the worker prompt; `src/goal/cli-worker.ts:265` writes worker artifacts; `src/goal/cli-worker.ts:278` builds CLI args; `src/goal/cli-worker.ts:327` launches the CLI process; `src/goal/cli-worker.ts:346` polls for early result files; `src/goal/cli-worker.ts:387` persists a canonical worker result; `src/goal/cli-worker.ts:401` conditionally repairs invalid result JSON; `src/goal/cli-worker.ts:502` classifies attempt outcome; `src/goal/cli-worker.ts:506` writes the attempt bundle; `src/goal/cli-worker.ts:540` defines prompt construction; `src/goal/cli-worker.ts:566` describes the worker role; `src/goal/cli-worker.ts:572` includes plan context; `src/goal/cli-worker.ts:576` injects lessons; `src/goal/cli-worker.ts:584` injects completed task summaries; `src/goal/cli-worker.ts:592` includes resume context; `src/goal/cli-worker.ts:601` names the assigned task; `src/goal/cli-worker.ts:647` defines the result protocol; `src/goal/cli-worker.ts:750` validates worker output; `src/goal/cli-worker.ts:819` reads worker result files. | definitely mine | working | core | The prompt always includes a generic build/test verification instruction (`src/goal/cli-worker.ts:642`) even when task constraints may forbid it; README should not imply workers automatically reconcile every constraint conflict without operator review. |
| Backend routing | CLI argument construction for Codex and Claude Code | Builds backend-specific CLI invocations: Codex runs `codex exec --json` with workspace-write sandbox, selected `--cd`, net setting, optional model, and approval-flag placement compatibility; Claude Code runs prompt mode with stream JSON output, allowed tool list, appended system prompt, and optional model. | `src/goal/cli-worker.ts:758` defines `buildCliArgs`; `src/goal/cli-worker.ts:782` starts the Codex path; `src/goal/cli-worker.ts:783` detects Codex approval flag placement; `src/goal/cli-worker.ts:786` uses `exec`; `src/goal/cli-worker.ts:787` enables JSON output; `src/goal/cli-worker.ts:789` sets workspace-write sandbox; `src/goal/cli-worker.ts:791` sets `--cd`; `src/goal/cli-worker.ts:795` sets `net.allowed=true`; `src/goal/cli-worker.ts:802` starts the Claude Code path; `src/goal/cli-worker.ts:803` builds allowed tools; `src/goal/cli-worker.ts:805` uses prompt mode; `src/goal/cli-worker.ts:807` requests stream JSON; `src/goal/cli-worker.ts:811` appends system prompt. | definitely mine | working | mention | The Codex invocation explicitly enables network access (`src/goal/cli-worker.ts:795`), while a given task may forbid network calls; public copy should frame this as a local CLI worker harness with guardrail prompts/artifacts, not as a hard sandbox policy unless capability enforcement is separately verified. |

## DAG & critical path

Authorship signal: local git history for `src/goal/dag-render.ts`, `src/goal/cpm.ts`, `src/goal/mermaid-render.ts`, `src/goal/types.ts`, and `src/goal/plan-order.ts` shows goal-system commits by `M O`, not upstream Moltbot commits. Relevant history includes `5d98307b8 2026-01-29 M O Add goal command: plan -> approve -> execute in sandbox`, `81da5f2ef 2026-01-30 M O goal: milestone 2 diagrams + output formats (mermaid/json), strict json mode`, `787411879 2026-01-30 M O milestone4: compute CPM schedule + annotate mermaid + add schedule outputs`, `80e3641cb 2026-02-02 M O fixed CPM ordering`, `b11cb16c4 2026-02-02 M O Mermaid Smithers Deluxe rendering + label normalization`, and later SmithersBot-specific Mermaid/status/backend label refinements.

| Sub-area | Feature name | What it does | Evidence | Upstream or mine | Maturity | README relevance | Risks or caveats |
|---|---|---|---|---|---|---|---|
| DAG construction | Plan steps as dependency graph | Represents each goal plan as steps with explicit `dependsOn` arrays, stores that graph in the persisted `Plan`, and renders a simple static dependency listing for plan display. This is the core data model that later CPM, Mermaid rendering, and worker scheduling consume. | `src/goal/types.ts:57` defines `PlanStep`; `src/goal/types.ts:62` stores `dependsOn: string[]`; `src/goal/types.ts:104` defines `Plan`; `src/goal/types.ts:107` stores `steps: PlanStep[]`; `src/goal/dag-render.ts:17` defines `renderAsciiDependencies`; `src/goal/dag-render.ts:20` iterates plan steps; `src/goal/dag-render.ts:21` reads `step.dependsOn`; `src/goal/format-output.ts:78` lists dependencies in Markdown output; `src/goal/format-output.ts:129` includes `dependsOn` in JSON output. | definitely mine | working | core | This is a dependency graph, not proof of parallel execution. Public copy should call it a DAG/task graph and avoid claiming concurrency unless separately verified. |
| Critical-path computation (CPM) | CPM schedule and critical-path extraction | Computes effective durations, validates/topologically sorts the graph, performs forward and backward CPM passes, calculates ES/EF/LS/LF/slack per step, marks critical steps, and returns one deterministic critical path plus total duration. | `src/goal/cpm.ts:3` defines per-step timing fields; `src/goal/cpm.ts:13` defines `CpmResult`; `src/goal/cpm.ts:20` defaults missing durations to one minute; `src/goal/cpm.ts:31` defines deterministic topological sort; `src/goal/cpm.ts:80` defines `computeCpm`; `src/goal/cpm.ts:97` obtains topological order; `src/goal/cpm.ts:99` starts the forward pass; `src/goal/cpm.ts:131` computes project duration; `src/goal/cpm.ts:138` starts the backward pass; `src/goal/cpm.ts:162` computes slack and `isCritical`; `src/goal/cpm.ts:177` extracts one critical path; `src/goal/format-output.ts:68` computes CPM for Markdown output; `src/goal/format-output.ts:86` prints the CPM schedule; `src/goal/format-output.ts:136` includes schedule data in JSON output. | definitely mine | working | core | CPM correctness appears intentionally deterministic and covered by source/tests, but this task did not run tests. It returns one valid critical path, not all possible critical paths (`src/goal/cpm.ts:16`). |
| Critical-path computation (CPM) | Critical-path-first ordering for display and scheduling | Computes static weighted scores from each step through its successors and orders step ids topologically while prioritizing higher critical-path score, successor locality, and original plan order. Mermaid rendering uses this for numbered node labels, and the worker executor uses the same ordering concepts for task selection. | `src/goal/plan-order.ts:5` builds successor adjacency; `src/goal/plan-order.ts:19` computes topological order by plan order; `src/goal/plan-order.ts:50` documents weighted critical-path scoring; `src/goal/plan-order.ts:55` defines `computeCriticalPathScores`; `src/goal/plan-order.ts:65` walks in reverse topological order; `src/goal/plan-order.ts:83` defines `orderStepIdsCriticalPathFirst`; `src/goal/plan-order.ts:101` chooses from ready nodes by score; `src/goal/plan-order.ts:108` filters max-score candidates; `src/goal/plan-order.ts:109` prefers successors of the last selected step; `src/goal/plan-order.ts:135` maps ordered ids back to steps; `src/goal/mermaid-render.ts:182` computes critical-path-first numeric labels; `src/goal/mermaid-render.ts:184` calls `orderStepIdsCriticalPathFirst`. | definitely mine | working | core | `plan-order.ts` falls back to original step order if it detects a cycle (`src/goal/plan-order.ts:43`), so validation must happen earlier; display ordering alone is not a validator. |
| Mermaid rendering | Mermaid dependency graph with status, durations, backend labels, and critical-path styling | Renders the plan as a Mermaid `flowchart TD`, escaping labels, normalizing descriptions, numbering nodes by critical-path-first order, drawing dependency edges, adding invisible links for disconnected plans, coloring nodes by execution status, showing estimated or actual duration, optionally showing backend labels, and styling critical-path edges thicker when CPM data is supplied. | `src/goal/mermaid-render.ts:9` defines the Mermaid init directive; `src/goal/mermaid-render.ts:23` defines status class styles; `src/goal/mermaid-render.ts:35` maps execution statuses to classes; `src/goal/mermaid-render.ts:51` escapes Mermaid labels; `src/goal/mermaid-render.ts:70` maps backend ids to display names; `src/goal/mermaid-render.ts:78` builds node duration/backend labels; `src/goal/mermaid-render.ts:122` normalizes labels; `src/goal/mermaid-render.ts:174` defines `renderMermaid`; `src/goal/mermaid-render.ts:180` starts `flowchart TD`; `src/goal/mermaid-render.ts:191` declares nodes; `src/goal/mermaid-render.ts:205` declares dependency edges; `src/goal/mermaid-render.ts:217` adds invisible links for no-edge layouts; `src/goal/mermaid-render.ts:223` emits class/link styles; `src/goal/mermaid-render.ts:233` applies critical-path link overrides; `src/goal/format-output.ts:98` adds Mermaid output when requested. | definitely mine | working | core | Mermaid output is source-backed, but rendering quality depends on the downstream Markdown/Telegram/browser renderer. Public screenshots or README demos should be generated from a real run before claiming visual polish. |
| Dependency validation | Unknown-dependency and cycle rejection | Validates planner output by rejecting dependencies that reference unknown step ids and rejecting cycles via Kahn's algorithm before a plan is accepted. CPM also performs its own unknown-dependency/cycle checks when computing schedules. | `src/goal/planner.ts:448` normalizes `dependsOn`; `src/goal/planner.ts:475` validates dependency references exist; `src/goal/planner.ts:479` throws for unknown dependencies; `src/goal/planner.ts:484` calls cycle detection; `src/goal/planner.ts:536` defines `detectCycles`; `src/goal/planner.ts:540` initializes in-degree from `dependsOn`; `src/goal/planner.ts:555` runs Kahn's algorithm; `src/goal/planner.ts:565` detects incomplete visitation; `src/goal/planner.ts:566` throws for dependency cycles; `src/goal/cpm.ts:39` checks unknown dependencies in CPM topo sort; `src/goal/cpm.ts:73` detects topological-sort failure; `src/goal/cpm.ts:74` throws for cycles. | definitely mine | working | core | Validation is structural, not semantic: it proves ids form an acyclic graph, not that the dependencies are the best or sufficient ordering. |
