# Stage 2H Deletion Ledger

| Path/subsystem deleted | Why removed | Evidence it was safe | Tests run | Future risk |
| --- | --- | --- | --- | --- |
| `extensions/_deferred/` | Temporary quarantine for non-v0 extensions should not remain in the public repo. | Workspace narrowed to `extensions/telegram` and `extensions/memory-core`; deleted tree was not part of v0 runtime. | `pnpm exec tsc -p tsconfig.json`; `pnpm build`; `pnpm install --frozen-lockfile`. | Deferred extension directories outside `_deferred` still need a later paired cleanup. |
| `skills/_deferred/` | Staged bundled skills should not ship or be enumerated as public v0 skills. | Bundled-skill walker enumerates the canonical `skills` root; deletion removes `_deferred` visibility without changing the walker. | `pnpm exec tsc -p tsconfig.json`; v0 test slice; build. | Remaining top-level skills on disk need future public/private ownership decisions. |
| `src/hooks/_deferred/` | Deferred hook packs were staging artifacts, not v0 hooks. | Grep found no named v0 importer in hook loader/config/goal/telegram surfaces; bundled hook README updated. | `pnpm vitest run src/hooks/`; v0 test slice. | Future hook catalog work should avoid recreating public quarantine paths. |
| Non-v0 extension/package `files[]` globs | Package should not ship unsupported non-v0 extension trees by default. | Workspace/package surface now lists only root, `ui`, Telegram extension, and memory-core extension. Frozen install passes. | `pnpm install --frozen-lockfile`; `pnpm build`; typecheck. | On-disk extension directories can still appear in source searches until later refactor/deletion. |
| Bundled skill package `files[]` globs | SmithersBot v0 should not market or ship a bundled skills catalog. | `skills/**` removed from package files; `_deferred` skill directory deleted. | `pnpm install --frozen-lockfile`; `pnpm build`; v0 test slice. | Developer-only skill material needs a later documentation/ownership pass. |
| Docker sandbox files and setup scripts | Docker-backed sandbox runtime is outside v0 and was old sandbox baggage. | Load-bearing path-policy/context/workspace modules were kept; runtime call sites rewired to non-Docker status. | Typecheck; build; lint; targeted sandbox-adjacent tests from the sandbox track; v0 and auto-reply test slices. | Browser/sandbox-browser decisions remain deferred. |
| Sandbox CLI and sandbox command modules/tests | Public CLI should not advertise Docker sandbox management after runtime deletion. | Sandbox subcommand removed from CLI registration; doctor/security/auto-reply call sites adjusted. | `pnpm vitest run src/cli/`; typecheck; build; lint. | Hidden/non-public CLI internals should be revisited after developer workflow review. |
| Docker sandbox labeler globs | Deleted paths should not remain in repo automation metadata. | `.github/labeler.yml` no longer points at removed Docker sandbox paths. | Typecheck/build/lint and git diff inspection. | None beyond future labeler maintenance. |
| `scripts/debug-mermaid-png.ts` and `debug:mermaid` | Debug-only mermaid PNG helper was not part of public v0. | Reference grep found no remaining consumer before deletion. | `pnpm build`. | Mermaid rendering still covered by goal tests and normal build paths. |
| Legacy auto-reply commands `/new`, `/reset`, `/stop`, `/approve`, `/restart`, `/bash` | These were legacy auto-reply commands outside the v0 Telegram goal/repo-chat surface. | Registrations/imports removed; shared session utilities kept where still used; Telegram goal/repo-chat slices passed. | `pnpm vitest run src/auto-reply/`; `pnpm vitest run src/telegram/ src/hooks/ src/goal/ src/repo-chat/ src/memory/`; typecheck. | If a private workflow still used these auto-reply commands, it must move to v0 goal/session commands. |
| Dedicated bash/approve auto-reply modules and tests | No v0 importer remained after command pruning. | Grep/import cleanup and typecheck confirmed no dangling references. | `pnpm vitest run src/auto-reply/`; typecheck. | None for v0; future shell execution should go through goal worker/tool policy paths. |
| Skipped legacy Discord/Slack/WhatsApp/iMessage auto-reply tests | Tests covered non-v0 legacy channels and were explicitly marked for pruning. | Remaining auto-reply and Telegram slices passed without skipped legacy-channel blocks. | `pnpm vitest run src/auto-reply/`; v0 test slice. | Non-Telegram channel support would need new explicit product decisions and fresh tests. |
| Outbound session branches for `matrix`, `nextcloud-talk`, `nostr`, `tlon`, `zalouser` | Dead deferred channels should not remain in outbound routing. | Persisted state search found no structured records that would crash after deletion; fallback resolver remains. | `pnpm vitest run src/infra/outbound/`; typecheck; build; lint. | Old unstructured notes may still mention channel names; they are not executable state. |
| Prior top-level Stage 2 reports/ledgers moved to `_archive/stage2-prior/` | Keep active audit root focused on current Stage 2H while preserving evidence. | Files were moved, not deleted; inventories, fragments, feature audit, and README verification artifacts remain in place. | `find RELEASE_AUDIT -maxdepth 3 -type f`; git status/diff review. | Public push should decide whether `_archive/` remains in repo or moves to another documented evidence location. |
