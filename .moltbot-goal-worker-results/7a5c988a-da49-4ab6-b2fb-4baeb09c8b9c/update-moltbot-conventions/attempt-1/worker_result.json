{"status":"complete","summary":"Updated CLAUDE.md (symlinked to AGENTS.md) with missing Moltbot conventions: added goal-test scoping guidance (`pnpm vitest run src/goal/` and `MOLTBOT_GOAL_TEST_SCOPE=1 pnpm test`) and explicit /goal verification sequence (`pnpm build`, goal Vitest run, `pnpm lint`, and `node scripts/run-node.mjs <args>`). Preserved all existing content and only added missing items. Verified with `pnpm build`, `pnpm vitest run src/goal/`, `pnpm lint`, and `node scripts/run-node.mjs goal --help` (all passed)."}
