{
  "status": "blocked",
  "question": "Cannot restart the gateway service from inside goal execution (hard deny on `systemctl --user restart`). The operator must restart and verify after this task completes.\n\nAll static checks pass: build OK, 155 tests pass (both test files), lint clean. Code review confirms:\n- step_blocked → stop-only keyboard (buildTaskBlockedInlineKeyboard)\n- fully_blocked → Resume+Stop keyboard (buildGoalBlockedInlineKeyboard)\n- TASK BLOCKED / GOAL BLOCKED captions use **bold** markdown\n- Ralph headers and labels are bold, blank lines between entries\n\nTo verify at runtime:\n1. `systemctl --user restart moltbot-gateway-dev.service`\n2. Run a goal that ralphs twice on a single task (triggers step_blocked). Confirm: Stop-only button, bold 'TASK BLOCKED', bold Ralph headers/labels, blank lines between ralph entries.\n3. Run a goal where ALL tasks block (triggers fully_blocked). Confirm: Resume+Stop buttons, bold 'GOAL BLOCKED'.\n4. Check run artifacts at ~/.moltbot/goals/<runId>/ for correct state."
}
