{ "status": "complete", "summary": "Swapped restart method order in src/telegram/gateway-restart.ts: triggerMoltbotRestart() (full systemd/launchctl restart) is now called first, with scheduleGatewaySigusr1Restart() (in-process SIGUSR1) as the fallback. Updated all 4 affected tests in gateway-restart.test.ts to match the new priority order. All 7 tests pass, build compiles, lint clean." }