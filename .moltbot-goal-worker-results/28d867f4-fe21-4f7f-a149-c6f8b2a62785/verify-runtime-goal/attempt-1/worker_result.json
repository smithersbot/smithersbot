{ "status": "complete", "summary": "End-to-end runtime verification passed. Ran a dry-run goal ('Create a hello-world shell script in /tmp/moltbot-test-goal/') via `env -u CLAUDECODE node moltbot.mjs goal ... --no-git-checkpoints --dry-run`. The CLI completed with exit code 0. Run artifacts at ~/.clawdbot-dev/goals/a890ab1b-c75b-48db-827b-1d454e3b1355/ confirmed: (1) run.json has state 'done' with a valid parsed plan containing 1 step, summary, buildGate, and all expected fields; (2) no plan-raw.txt was written, confirming extractJson() parsed the LLM response without PlanParseError. The CLAWDBOT_STATE_DIR env var pointed to ~/.clawdbot-dev (dev profile), which is why artifacts weren't in ~/.moltbot/goals/." }