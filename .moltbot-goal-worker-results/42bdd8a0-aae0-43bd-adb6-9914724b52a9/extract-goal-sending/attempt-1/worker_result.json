{"status":"complete","summary":"Extracted goal sending/persistence logic into src/telegram/goal-sending.ts (plan/background delivery, DAG PNG delivery, caption helpers, planner-fallback formatting, worker resolution, and all persist* helpers), updated src/telegram/goal-commands.ts to import/re-export the moved send helpers and consume extracted persistence APIs, and removed the inlined delivery block from goal-commands (reduced to 3051 lines). Verified with pnpm build and pnpm vitest run src/goal/ (both passed)."}
