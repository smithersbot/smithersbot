{"status":"complete","summary":"Added a whole-stdout JSON fallback in extractSessionIdFromStdout: after the existing line-by-line JSON scan loop, it now JSON.parse(stdout.trim()) and checks the same sessionIdFields list before returning undefined. Kept the function private and left the existing loop logic unchanged. Verification passed: pnpm build; pnpm vitest run src/repo-chat/repo-chat-worker.test.ts -t \"extracts session id from stdout json line\"."}
