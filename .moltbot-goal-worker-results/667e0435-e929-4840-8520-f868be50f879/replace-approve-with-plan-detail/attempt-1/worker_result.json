{ "status": "complete", "summary": "Updated Telegram goal approval inline keyboard to replace the duplicate top-right Approve button with 🔍 Plan Detail (`gD:<runIdPrefix>:<revision>`), expanded callback parsing to include `gD`, added a `gD` callback branch that resolves the run ID and calls `sendGoalDetailResponse`, ensured `gD` does not set a reaction emoji, and verified successfully with `pnpm build`." }
