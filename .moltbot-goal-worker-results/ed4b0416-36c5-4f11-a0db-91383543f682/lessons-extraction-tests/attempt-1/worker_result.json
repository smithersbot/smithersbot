{"status":"complete","summary":"Added extractRunLessons coverage in src/goal/lessons.test.ts with mocked CLI subprocess calls, temp-dir run artifacts (run.json + attempt bundles), assertions that existing lessons are injected for dedup context, persistence checks for extracted lessons (including source/runId/stepId fields), graceful fail-open cases for timeout/bad JSON/process errors, and no-corrections short-circuit behavior. Verified with: pnpm vitest run src/goal/lessons.test.ts && pnpm build."}
