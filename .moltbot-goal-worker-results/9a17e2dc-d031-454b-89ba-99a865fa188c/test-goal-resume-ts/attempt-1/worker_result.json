{ "status": "complete", "summary": "Added a regression test in src/commands/goal-resume.test.ts for --replan cancellation race: run starts in planning, runCliPlanning mock externally sets state to cancelled before returning a plan, goalResumeCommand returns { status: 'cancelled' }, and persisted run remains cancelled with plan null. Verified with pnpm vitest run src/commands/goal-resume.test.ts, pnpm build, and pnpm lint." }
