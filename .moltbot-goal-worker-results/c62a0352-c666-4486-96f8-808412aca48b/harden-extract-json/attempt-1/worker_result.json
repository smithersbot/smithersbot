{ "status": "complete", "summary": "Added a prose-preamble JSON fallback in src/goal/planner.ts extractJson(), strengthened PLAN_SYSTEM_PROMPT to require raw JSON-only output, added extractJson tests for preamble+bare-JSON success and invalid-brace failure in src/goal/planner.test.ts, and verified with pnpm vitest run src/goal/planner.test.ts, pnpm lint, pnpm build, plus a local node scripts/run-node.mjs goal --dry-run CLI self-check using a stub claude response with prose preamble + bare JSON." }
