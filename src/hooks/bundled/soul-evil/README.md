# SOUL Evil Hook

Small persona swap hook for SmithersBot.

See `src/hooks/bundled/soul-evil/HOOK.md`.

## Setup

1. `smithersbot hooks enable soul-evil`
2. Create `SOUL_EVIL.md` next to `SOUL.md` in your agent workspace.
3. Configure `hooks.internal.entries.soul-evil`.
